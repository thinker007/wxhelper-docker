version: "3.3"

services:
    wxhelper-docker:
        build: .
        restart: unless-stopped
        container_name: wxhelper-docker
        tty: true
        ports:
            - 8080:8080
            - 19088:19088
        extra_hosts:
            - "host.docker.internal:host-gateway"